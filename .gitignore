# OpenClaw internal
.openclaw/
workspace-state.json
.clawdhub/
.clawhub/
backup_log.txt
*.log
*.pid

# Safety: never push OpenClaw default templates (they lack real content)
# These files are ONLY safe when they contain real content (not template defaults)
/reference/
/BOOTSTRAP.md

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Temp
*.tmp
*~

# Runtime / auto-generated
memory/.dreams/
scripts/sync-windows.log
scripts/sync-windows.pid

# Local TTS / AI coding projects (not for sync)
dl_*.py
gen_*.py
run_*.py
run_*.sh
upload_*.py
server_*.py
check_*.py
即梦AI*
对话记录*
private/

# OpenClaw agent skill cache (auto-generated)
skills/.agents/

# Sync state (changes every cycle, don't track)
scripts/.sync-state.json
